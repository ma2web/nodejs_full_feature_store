import { defineMessages } from "react-intl";

const scope = "panel.dashboard.mainMenu";

const MainMenuMessages = defineMessages({
  main: {
    id: `${scope}.main`,
    defaultMessage: "Main",
  },
});
export default MainMenuMessages;
