import { defineMessages } from "react-intl";

const scope = "panel.products";

const ProductsMessages = defineMessages({
  products: {
    id: `${scope}.products`,
    defaultMessage: "Products",
  },
});

export default ProductsMessages;
