import { defineMessages } from "react-intl";

const scope = "panel.notifications";

const NotificationsMessages = defineMessages({
  notifications: {
    id: `${scope}.notifications`,
    defaultMessage: "Notifications",
  },
});

export default NotificationsMessages;
