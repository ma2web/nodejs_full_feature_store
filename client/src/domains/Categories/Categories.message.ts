import { defineMessages } from "react-intl";

const scope = "panel.categories";

const CategoriesMessages = defineMessages({
  categories: {
    id: `${scope}.categories`,
    defaultMessage: "Categories",
  },
});

export default CategoriesMessages;
